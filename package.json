{
  "name": "zenithfx",
  "version": "2.0.0",
  "description": "Zenith FX — Production Trading Platform",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Static + Serverless — no build step needed'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "nodemailer": "^6.9.8",
    "node-fetch": "^2.7.0",
    "googleapis": "^144.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
